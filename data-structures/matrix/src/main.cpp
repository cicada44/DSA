#include <matrix_t.h>

#include <iostream>

#include <string>

using namespace matrix_t;

int main() {
    // matrix<std::string> m1({{"1", "2"}, {"3", "4"}, {"5", "6"}});

    // matrix<std::string> m2({{"1", "1"}});

    // m2 = std::move(m1);

    // for (size_t i = 0; i != m2.rows(); ++i) {
    //     for (size_t j = 0; j != m2.cols(); ++j) std::cout << m2.at(i, j) << ' ';
    //     std::cout << '\n';
    // }
}
